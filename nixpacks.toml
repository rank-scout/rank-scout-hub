providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
# Wir kopieren deine Konfiguration über die Standard-Nginx-Konfig
cmd = "cp nginx.conf /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"